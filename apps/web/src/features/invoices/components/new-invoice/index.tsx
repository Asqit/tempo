import { useForm } from "react-hook-form";
import type { CreateInvoice } from "./types";

interface Props {
  defaultValues?: Partial<CreateInvoice>;
}

export function IssueInvoice({ defaultValues }: Props) {
  return <></>;
}
